const express = require("express");

const router = express.Router()

const {createActivity} = require("../controllers/Activitys")


router.post("/createActivity", createActivity);
module.exports = router
